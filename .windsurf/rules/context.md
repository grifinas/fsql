---
trigger: always_on
---

This project is a CLI tool. Goal is to help parse JSON files in a well known SQL format. Goal is NOT to create a fully functional SQL parser, but a simple subset of `SELECT` keyword functionalities.